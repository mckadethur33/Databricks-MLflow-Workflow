paths:
  raw: "/mnt/raw/tpcds"
  features: "/mnt/features/tpcds"
  predictions: "/mnt/predictions/tpcds"

model:
  name: "tpcds_customer_spend_model"

training:
  label_col: "total_store_sales"
  test_size: 0.2
  random_seed: 42
